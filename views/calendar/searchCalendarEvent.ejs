<!DOCTYPE html>
<html lang="de">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="../CSS/style.css">
  <title>Suchresultate Kalendereintrag</title>
</head>

<body>

  <div class="main-layout">
    <%- include("../partials/header") %>
    <main>
      <div class="container">

        <!-- flash message -->
        <% if(error && error.length) {%>
          <div class="alert alert-error" role="alert">
          <%= error %>
        </div>
        <% } else if(success){ %>
          <div class="" role="alert">
            <%= success %>
          </div>
          <% } %>

        <h1 class="title">Suchresultate</h1>

        <!-- Searchbar -->
        <form action="/kalender/suchen" role="search" method="POST">
          <label for="searchbar">
            <input type="search" name="searchTerm" id="searchbar" placeholder="Eintrag suchen..."
              class="search-input" value="<%=filteredSearchTerm%>" autofocus aria-label="Search">
          </label>
        </form>

        <div class="event-searchresults-wrapper">
          <% if(searchResults.length > 0) { %>
          <% searchResults.forEach(result => { %>
          <div class="event-searchresult" style="background-color: <%= result.color %>;">
            <div><span class="bold"><%=formatDate(result.startDate)%></span>
              <br>
              <% if (result.startEventTime) { %>
                <%=result.startEventTime %> - <%=result.endEventTime %>
                <% } %>              
            </div>
            <div><%=result.title%></div>
          </div>
          <% }) %>
          <% } else { %>
          <div class="text-center">
            <p>Keine Resultate für diesen Suchbegriff gefunden.
            </p>
            <br>
            <% } %>
          </div>

          <!-- back to calendar link -->
          <div class="flex-center">
          <a href="/kalender" class="page-navigation-link"><svg xmlns="http://www.w3.org/2000/svg"
            width="24" height="24" viewBox="0 0 512 512" class="inline-block-svg">
            <path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="48"
              d="M328 112L184 256l144 144" />
          </svg>zum Kalender</a>
        </div>
        </div>

      </div>
    </main>
    <%- include("../partials/footer") %>
  </div>

</body>

</html>